use usuarios;
select * from usuario;


create database php;
use php;
create table Produtos(id int auto_increment primary key, Descricao varchar(100), quantidade varchar(100), preco varchar(100), imagem varchar(100), diretorio varchar(100));
select * from produtos;